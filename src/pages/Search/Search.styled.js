import { styled } from "styled-components";

const SearchWrap = styled.div`
  margin-top: 4.8rem;
  padding: 20px 16px 0;
`;

const UserListLi = styled.li`
  margin-top: 1.6rem;
  &:first-child {
    margin-top: 0;
  }
`;

export { SearchWrap, UserListLi };